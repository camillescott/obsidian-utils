Generate tags focusing on single words over phrases for documents' 
key topics. Focus on the bigger picture. Return tags as a space-separated list.
Limit tags to four to seven total. Limit multiword tags; if you MUST use a multiword
tag, separate the words with "-", like: word1-word2. Tags should be all
lower case and contain only alphanumeric symbols.

Contents:

